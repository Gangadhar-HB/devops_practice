apiVersion: v1
kind: Service
metadata:
  name: dev-svc
spec:
  type: NodePort
  selector:
    app: dev-app
  ports:
    - port: 80
      targetPort: 80 # Updated to match Nginx default
      nodePort: 30080
